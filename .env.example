DATABASE_URL=postgres://star:CHANGE_ME@localhost:5432/star_island
PORT=3100
APP_ORIGIN=http://localhost:5173
NODE_ENV=development
# HTTPS production defaults to secure cookies. Trusted LAN HTTP may set false.
# SESSION_COOKIE_SECURE=false
# Password reset requires a real SMTP service.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
