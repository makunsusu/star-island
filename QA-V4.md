# 移动适配与角色 v4 验证记录

日期：2026-09-21。状态：本地预览，尚未发布 NAS。

## 已验证

- lint、14 项单元测试与生产构建通过。
- 隔离 PostgreSQL 的 `smoke:rig`：旧四槽兼容九槽、头饰与上衣独立穿戴、试穿隔离、整套穿戴、双会话存档同步、眨眼和表情恢复、答题反馈及减少动效通过。
- `smoke:gallery`：注册、教学赠装、浏览器通关、服务端 18 关、奖励防重、兑换、心愿、跨会话同步与金币流水通过。
- Chromium 九种视口：360×800、390×844、430×932、844×390、768×1024、820×1180、1024×768、1366×768、1920×1080。首页/商城无文档横向溢出；模拟接口的衣柜、变身、四种玩法、结算、家长页通过，数字键盘按钮至少 44×44。模拟接口不等同于真设备或完整网络测试。
- 144 个套装与配饰组合截图，720 个部件组合变换检查，12 个角色各五种情绪、三帧共 180 张动作截图已生成；检查了配饰矩阵和 12 组动作总览。修正了组件属性未透传导致的蒙版失效、披风领口遮挡、闭眼下缘残影。

## 尚未完成的验收

- 9 月 21 日重试 WebKit 下载仍在全部镜像遇到 TLS 连接中断，未执行；真实手机、平板的软键盘、旋转和浏览器行为未验证。
- 720 个混搭组合目前是数值检查，尚未逐张人工验收；不是“所有混搭都已无穿模”的证明。专属动作覆盖了起点、中段和结束帧，未检查连续动画的每一帧。
- 已从原图回溯恢复匹配的旧素材裁切信息，未能验证匹配的条目仍明确标记未恢复。草莓裙装和短裤已重新绘制为独立部件；其余没有裙装的套装对应无额外裙层。
- 图库同步本轮五张源图返回 socket hang up，无法确认入库。使用图库技能脚本和 `art-rig-v4-prompts.json` 中的中文描述重试；失败图不上传。
- NAS / Jenkins 发布未执行。部署目录、端口、学习数据与回滚脚本均未修改；后续视觉验收和设备验收后再发布。

截图位于忽略版本控制的 `work/v4/`。检查台可复现固定帧，见 `RIG.md`。

## 首页比例修正

用户截图暴露了窄屏两列网格的视觉问题：角色被压缩，表情按钮随网格行拉高。改为窄屏单列角色舞台，表情与操作在下方排列；在 360、390、600、760、820、1366px 宽度检查角色卡片截图、按钮高度 44px 和页面无横向溢出。lint 与构建通过。截图：`work/v4/hero-proportions-*.png`。
